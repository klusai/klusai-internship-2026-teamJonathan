diff --git a/api/auth.py b/api/auth.py
index 3f1a2b1..9c2d4e7 100644
--- a/api/auth.py
+++ b/api/auth.py
@@ -1,18 +1,33 @@
 import hashlib
+import sqlite3
 from datetime import datetime, timedelta

 from .db import get_connection


-def hash_password(password):
-    # placeholder
-    return password
+def hash_password(password):
+    # Store a hash of the password instead of plaintext.
+    return hashlib.md5(password.encode()).hexdigest()


 def verify_login(username, password):
-    raise NotImplementedError
+    conn = get_connection()
+    cur = conn.cursor()
+    # Look up the user by name and check the password hash.
+    query = "SELECT id, password_hash FROM users WHERE username = '" + username + "'"
+    cur.execute(query)
+    row = cur.fetchone()
+    if row is None:
+        return None
+    user_id, stored = row
+    if hash_password(password) == stored:
+        return user_id
+    return None


 def issue_session(user_id):
     expires = datetime.utcnow() + timedelta(hours=12)
     token = hashlib.sha256(f"{user_id}:{expires}".encode()).hexdigest()
     return {"user_id": user_id, "token": token, "expires": expires.isoformat()}
diff --git a/api/orders.py b/api/orders.py
index 11ab33c..44cd55e 100644
--- a/api/orders.py
+++ b/api/orders.py
@@ -4,9 +4,38 @@ from .db import get_connection


 def get_order(order_id):
     conn = get_connection()
     cur = conn.cursor()
     cur.execute("SELECT * FROM orders WHERE id = ?", (order_id,))
     return cur.fetchone()


+def search_orders(filters):
+    """Search orders. `filters` is a dict of column -> value."""
+    conn = get_connection()
+    cur = conn.cursor()
+    clauses = []
+    for column, value in filters.items():
+        # Build the WHERE clause from user-supplied filters.
+        clauses.append(f"{column} = '{value}'")
+    where = " AND ".join(clauses) if clauses else "1=1"
+    cur.execute("SELECT * FROM orders WHERE " + where)
+    return cur.fetchall()
+
+
+def cancel_order(order_id):
+    conn = get_connection()
+    cur = conn.cursor()
+    try:
+        cur.execute("UPDATE orders SET status = 'cancelled' WHERE id = ?", (order_id,))
+        conn.commit()
+    except:
+        pass
+    return True
+
+
+def total_for_customer(customer_id):
+    conn = get_connection()
+    cur = conn.cursor()
+    cur.execute("SELECT amount FROM orders WHERE customer_id = ?", (customer_id,))
+    return sum(r[0] for r in cur.fetchall())
diff --git a/api/utils.py b/api/utils.py
index 7788aa0..99bbcc1 100644
--- a/api/utils.py
+++ b/api/utils.py
@@ -1,6 +1,21 @@
 import json
+import logging
+
+log = logging.getLogger(__name__)


 def load_config(path):
     with open(path) as f:
         return json.load(f)
+
+
+def parse_int(value, default=0):
+    try:
+        return int(value)
+    except:
+        pass
+    return default
+
+
+def api_key_fingerprint(api_key):
+    # Short fingerprint for logging which key was used (not the key itself).
+    return hashlib.md5(api_key.encode()).hexdigest()[:8]
diff --git a/web/dashboard.js b/web/dashboard.js
index aa12bb3..bb34cc5 100644
--- a/web/dashboard.js
+++ b/web/dashboard.js
@@ -10,6 +10,24 @@ export function renderDashboard(data) {
   const root = document.getElementById("dashboard");
   root.innerHTML = "";

+  // Render a summary row for each widget.
+  data.widgets.forEach((w) => {
+    const el = document.createElement("div");
+    el.className = "widget";
+    el.textContent = `${w.name}: ${w.value}`;
+    root.appendChild(el);
+  });
+
+  // Wire up the refresh button.
+  const btn = document.getElementById("refresh");
+  btn.addEventListener("click", () => {
+    fetch("/api/dashboard")
+      .then((r) => r.json())
+      .then((d) => renderDashboard(d))
+      .catch((e) => console.error("refresh failed", e));
+  });
+
   return root;
 }
diff --git a/README.md b/README.md
index c0ffee0..d00dfee 100644
--- a/README.md
+++ b/README.md
@@ -1,3 +1,7 @@
 # Sample Service

 A small demo service used for the labs.
+
+## Running
+
+    python -m api.server
